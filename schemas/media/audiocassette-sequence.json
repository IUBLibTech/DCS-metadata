{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://dlib.indiana.edu/dwim/schemas/media/audiocassette-sequence@v1",
    "title": "DWIM Audiocassette Digitization Sequence",
    "description": "Capture / Playback details for an individual capture",
    "type": "object",
    "additionalProperties": false,
    "required": ["system", "label", "tape_speed", "signal_chain", "sound_field", "noise_reduction", "comments"],
    "properties": {
        "system": {
            "title": "System Information",
            "type": "object",
            "required": ["schema_id", "profile", "project_id", "physical_object_id", "sequence_id"],
            "properties": {    
                "schema_id": {
                    "const": "media/audiocassette-sequence@v1"
                },        
                "profile": {
                    "title": "Profile name",
                    "type": "string"
                },
                "project_id": {
                    "title": "Project ID",
                    "description": "The unique ID that represents this project",
                    "type": "string"
                },
                "physical_object_id": {
                    "title": "Physical Object ID",
                    "description": "The ID for the physical object that this describes",
                    "type": "string"
                },
                "sequence_id": {
                    "title": "Sequence ID",
                    "description": "The sequence number",
                    "type": "integer"
                }
            }
        },
        "label": {
            "title": "Label",
            "description": "Label information to identify the part of the media being digitized",
            "type": "string",
            "default": "n/a"
        },
        "tape_speed": {
            "title": "Tape Speed",
            "description": "Recording speed used in inches per second",
            "type": "string",
            "enum": ["3.75 ips", "1.875 ips", "0.9375 ips", "0.46875 ips", "unknown"],
            "default": "1.875 ips"
        },
        "signal_chain": {
            "title": "Signal Chain",
            "description": "Information about the equipment used to capture the media",
            "const": "TBD"
        },
        "sound_field": {
            "title": "Sound Field",
            "description": "What kind of sound field was captured",
            "enum": ["stereo", "mono"],
            "default": "stereo"
        },
        "noise_reduction": {
            "title": "Noise reduction",
            "description": "Type of noise reduction used during the capture",
            "type": "string",
            "enum": ["none", "Dolby Unknown", "Dolby B", "Dolby C"],
            "default": "none"
        },
        
        "comments": {
            "title": "Digitization Notes",
            "description": "Comments about anything strange or abnormal about the content",
            "type": "string",
            "default": "no comment"
        }
    }
}
